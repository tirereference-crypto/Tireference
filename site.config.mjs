/**
 * Single source of truth for Tire Reference's preferred production origin.
 * Canonicals, sitemaps, robots, structured data, and build validation consume
 * this value directly or through src/lib/seo/constants.ts.
 */
export const PRODUCTION_ORIGIN = 'https://tirereference.com';
